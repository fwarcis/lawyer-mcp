# Table: Взаимодействия

## Fields
  - "Взаимодействие" [id: flddjHkXSvYCGcscwY1] (Single line text) [PRIMARY]
  - "Дата" [id: fldaeRGPwMeQKekIO06] (Date/Time)
  - "Канал" [id: fldNw8TUMtIynxmtL16] (Single select (options: Звонок, Email, Встреча, Мессенджер, Суд))
  - "Результат" [id: fldLsfCWMSc4MI0twGr] (Long text / Rich text)
  - "Следующий шаг" [id: fldtaMmrRPXSBKJKkvq] (Long text / Rich text)
  - "Следующий контакт" [id: fldpKmxdpVvxKspPoX9] (Date/Time)
  - "Ответственный" [id: fldGpC9YrZtOfmFwrpV] (User reference)
  - "Клиент" [id: fld9AW1SdsJOv7Zhy4v] (Link to another table)
  - "Дело" [id: fldSQhQatlSxpxsRPug] (Link to another table)

## API Information
- Base URL: https://app.teable.ai
- Table ID: tblo7bC4S6ytz3Gnb7b
- API Endpoint: https://app.teable.ai/api/table/tblo7bC4S6ytz3Gnb7b/record

## How to Interact with This Table

### Read Records (GET)
```
GET https://app.teable.ai/api/table/tblo7bC4S6ytz3Gnb7b/record?fieldKeyType=name
Authorization: Bearer YOUR_API_TOKEN
```

Query Parameters:
- filter: JSON filter object for filtering records (⚠️ use field ID, not name)
- orderBy: JSON array for sorting (⚠️ use field ID, not name)
- search: Search text across all fields
- take: Number of records to return (default: 100, max: 1000)
- skip: Number of records to skip for pagination

**Important**: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name. Field IDs are listed above in the Fields section.

### Create Record (POST)
```
POST https://app.teable.ai/api/table/tblo7bC4S6ytz3Gnb7b/record
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        // Editable fields: "Взаимодействие", "Дата", "Канал", "Результат", "Следующий шаг", "Следующий контакт", "Ответственный", "Клиент", "Дело"
      }
    }
  ]
}
```

### Update Record (PATCH)
```
PATCH https://app.teable.ai/api/table/tblo7bC4S6ytz3Gnb7b/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "record": {
    "fields": {
      // Include only fields you want to update
    }
  }
}
```

### Delete Record (DELETE)
```
DELETE https://app.teable.ai/api/table/tblo7bC4S6ytz3Gnb7b/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
```

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use fieldKeyType=name to reference fields by their display name in request/response body
- **Important**: filter and orderBy parameters MUST use field IDs (e.g., "fldXXXX"), not field names
- Dates should be in ISO 8601 format
- For select fields, use the exact option names listed
- For link fields, provide an array of record IDs from the linked table

