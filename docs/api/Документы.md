# Table: Документы

## Fields
  - "Документ" [id: fldTHbto63CekXE73PO] (Single line text) [PRIMARY]
  - "Файл" [id: fldeHfBzovvYkP6Nr2n] (File attachments)
  - "Тип" [id: fldDHBH8B4xsmYt88oZ] (Single select (options: Договор, Доверенность, Заявление, Доказательство, Судебный акт, Счет, Другое))
  - "Дата получения" [id: fld9dvap4i1cjozYgAt] (Date/Time)
  - "Конфиденциально" [id: fldqNeHKfcr4E0IbwCe] (Checkbox (true/false))
  - "Срок действия" [id: fld20wLIeXBm1WxbLkU] (Date/Time)
  - "Заметки" [id: fldYnHplxrZgJUg1efc] (Long text / Rich text)
  - "Клиент" [id: fldvxRnOG2fmgzID3bK] (Link to another table)
  - "Дело" [id: fldLkl0yTmiKfrs1vzp] (Link to another table)

## API Information
- Base URL: https://app.teable.ai
- Table ID: tblKDlLAQ8nGwo0qlYW
- API Endpoint: https://app.teable.ai/api/table/tblKDlLAQ8nGwo0qlYW/record

## How to Interact with This Table

### Read Records (GET)
```
GET https://app.teable.ai/api/table/tblKDlLAQ8nGwo0qlYW/record?fieldKeyType=name
Authorization: Bearer YOUR_API_TOKEN
```

Query Parameters:
- filter: JSON filter object for filtering records (⚠️ use field ID, not name)
- orderBy: JSON array for sorting (⚠️ use field ID, not name)
- search: Search text across all fields
- take: Number of records to return (default: 100, max: 1000)
- skip: Number of records to skip for pagination

**Important**: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name. Field IDs are listed above in the Fields section.

### Create Record (POST)
```
POST https://app.teable.ai/api/table/tblKDlLAQ8nGwo0qlYW/record
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        // Editable fields: "Документ", "Файл", "Тип", "Дата получения", "Конфиденциально", "Срок действия", "Заметки", "Клиент", "Дело"
      }
    }
  ]
}
```

### Update Record (PATCH)
```
PATCH https://app.teable.ai/api/table/tblKDlLAQ8nGwo0qlYW/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "record": {
    "fields": {
      // Include only fields you want to update
    }
  }
}
```

### Delete Record (DELETE)
```
DELETE https://app.teable.ai/api/table/tblKDlLAQ8nGwo0qlYW/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
```

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use fieldKeyType=name to reference fields by their display name in request/response body
- **Important**: filter and orderBy parameters MUST use field IDs (e.g., "fldXXXX"), not field names
- Dates should be in ISO 8601 format
- For select fields, use the exact option names listed
- For link fields, provide an array of record IDs from the linked table

