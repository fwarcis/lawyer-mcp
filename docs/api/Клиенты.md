# Table: Клиенты

## Fields
  - "Клиент" [id: fldYoptHjzf9ctp9hLW] (Single line text) [PRIMARY]
  - "Телефон" [id: fldThxhxNmnp2YFhr9O] (Single line text)
  - "Статус клиента" [id: fldn5GZY4t6unLruZaz] (Single select (options: Потенциальный, Активный, Неактивный, Архив))
  - "Тип клиента" [id: fldvkg3IjsSMBxlvKEF] (Single select (options: Физическое лицо, Компания))
  - "Email" [id: fldlV8CAprAzKFsFtdD] (Single line text)
  - "ИИН / БИН" [id: fldflp73HIEBdozZ2q8] (Single line text)
  - "Адрес" [id: fldcBX4HV59tcwi17ye] (Long text / Rich text)
  - "Источник" [id: fldHJ6UCesQFhjzdctX] (Single select (options: Рекомендация, Сайт, Соцсети, Повторное обращение, Другое))
  - "Проверка конфликта" [id: fldKwAUrUPkOHgQjIWx] (Single select (options: Не проверено, Конфликт не выявлен, Конфликт выявлен))
  - "Ответственный юрист" [id: fldK5Tz0yd8jBymG2vL] (User reference)
  - "Последний контакт" [id: flddw3uuMW5SnkXzh3h] (Date/Time)
  - "Дата добавления" [id: fldRLxZnbWzBnBkqLHJ] (Created time (auto-generated)) [READ-ONLY]
  - "Заметки" [id: fldCrFhoBxh5wMA547h] (Long text / Rich text)
  - "Дела" [id: fld0hD70Fr8SaeL9L1Q] (Link to another table)
  - "Документы" [id: fldSQZM2cHRXaemBOTF] (Link to another table)
  - "Взаимодействия" [id: fldZRLMXTOW9aCZ1n17] (Link to another table)
  - "Задачи" [id: fldRj1DLJIjVFlqpl4s] (Link to another table)

## API Information
- Base URL: https://app.teable.ai
- Table ID: tblnRVk5GfCN2JNEovH
- API Endpoint: https://app.teable.ai/api/table/tblnRVk5GfCN2JNEovH/record

## How to Interact with This Table

### Read Records (GET)
```
GET https://app.teable.ai/api/table/tblnRVk5GfCN2JNEovH/record?fieldKeyType=name
Authorization: Bearer YOUR_API_TOKEN
```

Query Parameters:
- filter: JSON filter object for filtering records (⚠️ use field ID, not name)
- orderBy: JSON array for sorting (⚠️ use field ID, not name)
- search: Search text across all fields
- take: Number of records to return (default: 100, max: 1000)
- skip: Number of records to skip for pagination

**Important**: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name. Field IDs are listed above in the Fields section.

### Create Record (POST)
```
POST https://app.teable.ai/api/table/tblnRVk5GfCN2JNEovH/record
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        // Editable fields: "Клиент", "Телефон", "Статус клиента", "Тип клиента", "Email", "ИИН / БИН", "Адрес", "Источник", "Проверка конфликта", "Ответственный юрист", "Последний контакт", "Заметки", "Дела", "Документы", "Взаимодействия", "Задачи"
      }
    }
  ]
}
```

### Update Record (PATCH)
```
PATCH https://app.teable.ai/api/table/tblnRVk5GfCN2JNEovH/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "record": {
    "fields": {
      // Include only fields you want to update
    }
  }
}
```

### Delete Record (DELETE)
```
DELETE https://app.teable.ai/api/table/tblnRVk5GfCN2JNEovH/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
```

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use fieldKeyType=name to reference fields by their display name in request/response body
- **Important**: filter and orderBy parameters MUST use field IDs (e.g., "fldXXXX"), not field names
- Dates should be in ISO 8601 format
- For select fields, use the exact option names listed
- For link fields, provide an array of record IDs from the linked table

