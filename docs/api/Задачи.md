# Table: Задачи

## Fields
  - "Задача" [id: fld3FnkCy8P9Lc0IR6q] (Single line text) [PRIMARY]
  - "Статус" [id: fldEppAOzkyrk4zFbrS] (Single select (options: К выполнению, В работе, Ожидание, Выполнено, Отменено))
  - "Срок" [id: fldsFNum5X5jbbNWLwx] (Date/Time)
  - "Приоритет" [id: fldcNDNBpx2zqICDWaB] (Single select (options: Низкий, Обычный, Высокий, Срочный))
  - "Тип" [id: fldyFC1ncm9shDcrVPv] (Single select (options: Звонок, Документ, Заседание, Встреча, Исследование, Другое))
  - "Ответственный" [id: fldmbRAo5fImT8J4scA] (User reference)
  - "Выполнено" [id: fldHLvsSuq7HhEs3flh] (Checkbox (true/false))
  - "Заметки" [id: fldJkSUK2fSsEvxfLKK] (Long text / Rich text)
  - "Дело" [id: fldCo2N7pWNDERIPjLO] (Link to another table)
  - "Клиент" [id: fldHfWuTdGYXWlZbci4] (Link to another table)

## API Information
- Base URL: https://app.teable.ai
- Table ID: tblzFRUTxwbMTXyIeNM
- API Endpoint: https://app.teable.ai/api/table/tblzFRUTxwbMTXyIeNM/record

## How to Interact with This Table

### Read Records (GET)
```
GET https://app.teable.ai/api/table/tblzFRUTxwbMTXyIeNM/record?fieldKeyType=name
Authorization: Bearer YOUR_API_TOKEN
```

Query Parameters:
- filter: JSON filter object for filtering records (⚠️ use field ID, not name)
- orderBy: JSON array for sorting (⚠️ use field ID, not name)
- search: Search text across all fields
- take: Number of records to return (default: 100, max: 1000)
- skip: Number of records to skip for pagination

**Important**: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name. Field IDs are listed above in the Fields section.

### Create Record (POST)
```
POST https://app.teable.ai/api/table/tblzFRUTxwbMTXyIeNM/record
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        // Editable fields: "Задача", "Статус", "Срок", "Приоритет", "Тип", "Ответственный", "Выполнено", "Заметки", "Дело", "Клиент"
      }
    }
  ]
}
```

### Update Record (PATCH)
```
PATCH https://app.teable.ai/api/table/tblzFRUTxwbMTXyIeNM/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
Content-Type: application/json

{
  "fieldKeyType": "name",
  "record": {
    "fields": {
      // Include only fields you want to update
    }
  }
}
```

### Delete Record (DELETE)
```
DELETE https://app.teable.ai/api/table/tblzFRUTxwbMTXyIeNM/record/{recordId}
Authorization: Bearer YOUR_API_TOKEN
```

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use fieldKeyType=name to reference fields by their display name in request/response body
- **Important**: filter and orderBy parameters MUST use field IDs (e.g., "fldXXXX"), not field names
- Dates should be in ISO 8601 format
- For select fields, use the exact option names listed
- For link fields, provide an array of record IDs from the linked table

